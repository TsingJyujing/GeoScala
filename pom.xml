<project>
    <modelVersion>4.0.0</modelVersion>


    <properties>
        <!--Scala version = main.sub -->
        <scala.version.main>2.12</scala.version.main>
        <scala.version.sub>3</scala.version.sub>

    </properties>

    <groupId>tsingjyujing</groupId>
    <artifactId>geo-library-${scala.version.main}</artifactId>
    <version>1.1</version>
    <packaging>jar</packaging>

    <dependencies>
        <!-- https://mvnrepository.com/artifact/org.scala-lang/scala-library -->
        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-library</artifactId>
            <version>${scala.version.main}.${scala.version.sub}</version>
        </dependency>

        <!-- https://mvnrepository.com/artifact/com.google.guava/guava -->
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>12.0.1</version>
        </dependency>


    </dependencies>


</project>